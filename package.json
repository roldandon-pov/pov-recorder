{
  "name": "pov-rtmp-encoder",
  "version": "0.1.0",
  "private": true,
  "description": "POV LIVE encoder + recorder de nube (show rápido → Mux).",
  "main": "encoder.js",
  "scripts": {
    "start": "node encoder.js",
    "server": "node server.js",
    "quick-record": "node quick-record.mjs"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "engines": {
    "node": ">=18"
  }
}
